# Pinned dependencies for building the IDM documentation site.
# Reference pages are generated by scripts/gen_schema_docs.py, which uses only
# the Python standard library, so the build needs MkDocs Material and nothing else.
mkdocs-material==9.7.6
