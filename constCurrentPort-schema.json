{
  "$comment": "This schema describes a port with a constant current characteristic.",
  "schema": {
    "type": "object",
    "properties": {
      "portType": {
        "const": "constantCurrent"
      },
      "targetCurrentOut": { "$ref": "currentRating-schema.json#/schema" },
      "openCctVolts": { "$ref": "voltageRating-schema.json#/schema" }
    }
  }
}

